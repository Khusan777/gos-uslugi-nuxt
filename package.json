{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "start-build": "nuxi build && node .output/server/index.mjs",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "analyze": "npx nuxi analyze",
    "project-info": "npx nuxi info",
    "project-hard-reset": "rm -rf node_modules yarn.lock && yarn cache clean && yarn && yarn dev"
  },
  "devDependencies": {
    "nuxt": "^3.11.2",
    "sass": "^1.77.3",
    "sass-loader": "^14.2.1"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.1",
    "swiper": "^10.3.1"
  }
}
